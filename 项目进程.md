# 智观·古建 (Zhiguan Gujian) - 项目执行进程

## 执行日期: 2026-05-13

### 总体进度
- [x] 需求分析 - 已读取 古建AI平台项目提示词生成.md
- [x] Task 1: 后端包结构改造 com.example.demo → com.zhiguan.gujian
- [x] Task 2: 实体类与Mapper创建 (9张表)
- [x] Task 3: Spring Security + JWT + 配置类
- [x] Task 4: Service业务层实现
- [x] Task 5: REST API控制器
- [x] Task 6: 前端Vue3界面重建

### 执行日志
| 时间 | 任务 | 状态 |
|------|------|------|
| 2026-05-13 22:07 | 初始化项目分析 | 完成 |
| 2026-05-13 22:10 | 后端包结构改造 | 完成 |
| 2026-05-13 22:15 | 实体类+Mapper创建 | 完成 |
| 2026-05-13 22:20 | Security+JWT+配置 | 完成 |
| 2026-05-13 22:25 | Service层实现 | 完成 |
| 2026-05-13 22:30 | Controller层实现 | 完成 |
| 2026-05-13 22:35 | 前端界面重建 | 完成 |
| 2026-05-13 22:40 | Maven编译验证 | 通过 |

---

## 项目结构总览

### 后端 (Spring Boot 3.2 + Java 17)
```
backend/src/main/java/com/zhiguan/gujian/
├── ZhiguanApplication.java          # 启动类 (@EnableAsync)
├── annotation/RateLimit.java        # VGGT限流注解
├── aop/aspect/RateLimitAspect.java  # 每日5次限制切面
├── config/
│   ├── AsyncConfig.java             # 异步线程池
│   ├── CorsConfig.java              # 跨域配置
│   ├── MybatisPlusConfig.java       # 分页+自动填充
│   ├── RestTemplateConfig.java      # 远端API调用
│   ├── Result.java                  # 统一响应格式
│   ├── SecurityConfig.java          # Spring Security + JWT
│   └── WebMvcConfig.java            # 本地文件映射 /assets/**
├── constant/
│   ├── CulturalLexicon.java         # 古建文化词库
│   └── TaskStatusEnum.java          # 任务状态枚举
├── controller/
│   ├── AuthController.java          # POST /api/v1/auth/login|register
│   ├── CommunityController.java     # GET|POST /api/v1/posts, comments, likes, follows
│   ├── HuanZhuController.java       # POST /api/v1/tasks/huanzhu, GET status
│   ├── NotificationController.java  # GET|PUT /api/v1/notifications
│   └── ZhiXiController.java         # POST /api/v1/analysis/zhixi, restoration预留
├── dto/request/                     # LoginReq, RegisterReq, HuanZhuReq, CommentReq, LikeReq, CreatePostReq
├── dto/response/                    # PostBriefResp, PostDetailResp, CommentResp, TaskStatusResp, NotificationResp
├── exception/
│   ├── CulturalApiException.java
│   └── GlobalExceptionHandler.java
├── filter/JwtAuthenticationFilter.java
├── mapper/                          # 9个Mapper接口 (MyBatis-Plus BaseMapper)
├── model/                           # 9个实体类 (对应数据库表)
├── security/JwtUtil.java
├── service/
│   ├── AuthService.java / impl
│   ├── CommunityService.java / impl
│   ├── NotificationService.java / impl
│   └── TaskOrchestrationService.java / impl  # @Async 异步状态机
└── utils/
    ├── FileUtil.java                # 本地文件存储
    └── PromptEnhancerUtil.java      # 幻筑Prompt文化增强
```

### 前端 (Vue 3 + Vite + Element Plus + Pinia)
```
frontend/src/
├── main.js
├── App.vue                          # 导航栏 + 数字锦盒Modal
├── assets/style.css                 # 中国传统色彩CSS变量
├── router/index.js                  # 7个路由 + 鉴权守卫
├── api/
│   ├── request.js                   # Axios封装
│   ├── auth.js, community.js, huanzhu.js, zhixi.js, notification.js
├── stores/
│   ├── user.js                      # 用户状态 (Pinia)
│   └── notification.js              # 通知状态 + 锦盒逻辑
└── views/
    ├── LoginView.vue                # 登录/注册
    ├── CommunityFeedView.vue        # 首页瀑布流 (仅2D封面)
    ├── PostDetailView.vue           # 沉浸展示 (<model-viewer>)
    ├── HuanZhuView.vue              # 一键幻筑 + Prompt引导 + 轮询
    ├── ZhiXiView.vue                # 古建智析 + 文化转译
    └── NotificationListView.vue     # 通知中心
```

---

## 执行日期: 2026-05-14

### 总体进度
- [x] Task 1: Python VGGT API (api_server.py) 创建 — FastAPI 监听 8000 端口，模拟 VGGT 结构推断
- [x] Task 2: ZhiXiController 改造 — 移除离线演示逻辑，RestTemplate 对接 Python VGGT API
- [x] Task 3: AncientDictUtil 文化词库 — 建筑类型/朝代/屋顶/色彩四层匹配增强
- [x] Task 4: TaskOrchestrationServiceImpl 完善 — 接入 AncientDictUtil + 数字锦盒通知文案
- [x] Task 5: Maven 编译验证通过

### 执行日志
| 时间 | 任务 | 状态 |
|------|------|------|
| 2026-05-14 11:00 | 创建 Python VGGT api_server.py | 完成 |
| 2026-05-14 11:10 | 改造 ZhiXiController (RestTemplate 对接) | 完成 |
| 2026-05-14 11:15 | 新建 AncientDictUtil.java 文化词库 | 完成 |
| 2026-05-14 11:20 | 完善 TaskOrchestrationServiceImpl 异步链路 | 完成 |
| 2026-05-14 11:25 | Maven 编译验证 | 通过 |

### 详细变更清单

#### 新增文件
| 文件 | 说明 |
|------|------|
| `D:\Code\Python\vggt-main\api_server.py` | FastAPI 服务，POST /v1/analyze 接收图片，模拟 VGGT 返回 structural_elements |
| `backend/.../utils/AncientDictUtil.java` | 静态文化词库增强工具，支持建筑类型/朝代/屋顶/色彩四层智能匹配 |

#### 修改文件
| 文件 | 变更内容 |
|------|------|
| `backend/.../controller/ZhiXiController.java` | 注入 RestTemplate，移除 AnalysisDemoMapper 离线读取，将图片转发至 Python VGGT API，保留 @RateLimit(5) |
| `backend/.../service/impl/TaskOrchestrationServiceImpl.java` | PromptEnhancerUtil → AncientDictUtil，站内信文案更新为"您的古建数字锦盒已送达，请拆阅"，补充完整中文注释说明模块交互机制 |

### 系统模块交互架构（当前 V1.2）

```
用户浏览器 (Vue 3 :5173)
    │
    ├── 社区/帖子/评论/点赞 ──→ Spring Boot (:8080) ──→ MySQL
    │
    ├── 一键幻筑 POST /huanzhu
    │   ├── 同步返回 taskId + PENDING
    │   └── @Async 后台线程:
    │       AncientDictUtil.enhance(prompt)
    │       → RUNNING → 模拟 AI 3D 生成
    │       → SUCCESS → model_asset (封面+GLB) + notification (数字锦盒)
    │
    └── 古建智析 POST /zhixi (@RateLimit 5次/日)
        └── RestTemplate ──→ Python FastAPI (:8000) /v1/analyze
            └── 返回 structural_elements (斗栱/柱础/檐椽/鸱吻)
```

---

## 执行日期: 2026-05-14 (第二批次 — RBAC + 社区互动 + 前端个人中心/审核台)

### 总体进度
- [x] Task 1: User/Post 数据模型扩展 — role 字段 + status 审核状态字段
- [x] Task 2: Spring Security RBAC 确立 — JWT 携带 role + GrantedAuthority + @EnableMethodSecurity
- [x] Task 3: AdminController 审核 API — GET/PUT /api/v1/admin/posts/**
- [x] Task 4: CommunityService 完善 — 发帖默认 PENDING + 信息流仅展示 APPROVED + 用户帖子/点赞查询
- [x] Task 5: 前端个人中心 UserProfileView.vue — 三 Tab (我的帖子/点赞/数字锦盒)
- [x] Task 6: 前端设置中心 SettingsView.vue — 资料编辑/密码修改/通知开关骨架
- [x] Task 7: 前端审核工作台 AdminDashboardView.vue — 通过/驳回 + 路由权限守卫
- [x] Task 8: Maven 编译验证通过

### 执行日志
| 时间 | 任务 | 状态 |
|------|------|------|
| 2026-05-14 12:00 | User 模型增加 role 字段 | 完成 |
| 2026-05-14 12:05 | Post 模型增加 status + reject_reason | 完成 |
| 2026-05-14 12:10 | JwtUtil + JwtFilter + SecurityConfig RBAC | 完成 |
| 2026-05-14 12:15 | AdminController 审核接口 | 完成 |
| 2026-05-14 12:20 | CommunityService 内容审核逻辑 | 完成 |
| 2026-05-14 12:25 | UserProfileView / SettingsView | 完成 |
| 2026-05-14 12:30 | AdminDashboardView + 路由守卫 | 完成 |
| 2026-05-14 12:35 | Maven 编译验证 | 通过 |

### 详细变更清单

#### 新增文件
| 文件 | 说明 |
|------|------|
| `backend/.../controller/AdminController.java` | 管理员审核接口，@PreAuthorize("hasRole('ADMIN')") 双层保护 |
| `frontend/src/views/UserProfileView.vue` | 个人中心 — 头像/昵称/文化签名 + 我的帖子/点赞/锦盒三 Tab |
| `frontend/src/views/SettingsView.vue` | 设置中心 — 资料编辑/密码修改/通知开关静态骨架 |
| `frontend/src/views/AdminDashboardView.vue` | 审核工作台 — 待审核列表 + 通过/驳回操作 |
| `frontend/src/api/admin.js` | 管理员 API 封装 (getPendingPosts / auditPost) |

#### 修改文件
| 文件 | 变更内容 |
|------|------|
| `model/User.java` | 新增 `role` 字段 (USER/ADMIN) |
| `model/Post.java` | 新增 `status` (PENDING/APPROVED/REJECTED) + `rejectReason` 字段 |
| `security/JwtUtil.java` | generateToken() 增加 role 参数；新增 getRoleFromToken() |
| `filter/JwtAuthenticationFilter.java` | 将 role 映射为 SimpleGrantedAuthority (ROLE_ADMIN/ROLE_USER) |
| `config/SecurityConfig.java` | 新增 @EnableMethodSecurity；/api/v1/admin/** → hasRole('ADMIN') |
| `service/impl/AuthServiceImpl.java` | 注册默认 role=USER；登录 JWT 携带 role |
| `service/impl/CommunityServiceImpl.java` | 信息流过滤 APPROVED；发帖默认 PENDING；新增 auditPost/getPendingPosts/getUserPosts/getUserLikedPosts |
| `service/CommunityService.java` | 接口新增审核与用户查询方法声明 |
| `controller/CommunityController.java` | 新增 /users/me (GET/PUT)、/users/me/password、/users/me/posts、/users/me/likes |
| `resources/init.sql` | CREATE TABLE 增加 role/status/reject_reason 字段；新增 V1.0→V1.2 ALTER 迁移语句；新增管理员种子账号 (admin/admin123) |
| `frontend/src/stores/user.js` | 新增 role/isAdmin 状态；新增 parseToken() 从 JWT 解析角色 |
| `frontend/src/router/index.js` | 新增 /profile /settings /admin 路由；admin 路由 requiresAdmin 前端守卫 |
| `frontend/src/api/auth.js` | 新增 getCurrentUser/updateProfile/changePassword/getMyPosts/getMyLikes |
| `frontend/src/App.vue` | 下拉菜单新增个人中心/设置/审核工作台入口；仅管理员可见审核入口 |

### RBAC 权限模型 (V1.2)

```
                    ┌─────────────────────────┐
                    │   Spring Security        │
                    │   @EnableMethodSecurity  │
                    └───────────┬─────────────┘
                                │
            ┌───────────────────┼───────────────────┐
            │                   │                   │
    /api/v1/auth/**      /api/v1/admin/**      其余 /api/v1/**
    公开访问              hasRole('ADMIN')       需登录认证
                            │
                    ┌───────┴───────┐
                    │  @PreAuthorize │  ← 方法级双重保护
                    │  ("hasRole     │
                    │   ('ADMIN')")  │
                    └───────────────┘

内容审核状态机:
  用户发帖 → PENDING (待审核)
                ├── ADMIN 通过 → APPROVED (展示于信息流)
                └── ADMIN 驳回 → REJECTED (记录驳回原因)
```

### 新增/变更 API 端点 (V1.2)

| 模块 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 个人 | GET | /api/v1/users/me | 获取当前用户信息 |
| 个人 | PUT | /api/v1/users/me | 更新个人资料 |
| 个人 | PUT | /api/v1/users/me/password | 修改密码 |
| 个人 | GET | /api/v1/users/me/posts | 我的帖子列表 |
| 个人 | GET | /api/v1/users/me/likes | 我点赞的帖子 |
| 管理 | GET | /api/v1/admin/posts/pending | 待审核帖子列表 |
| 管理 | PUT | /api/v1/admin/posts/{id}/audit | 审核通过/驳回 |

---

## 启动步骤

### 1. 数据库 (已由用户创建)
```sql
-- 在 MySQL 中执行 init.sql
source backend/src/main/resources/init.sql
```

### 2. 启动后端
```bash
cd backend
mvn spring-boot:run
# 访问: http://localhost:8080/api/v1/analysis/zhixi/demos
```

### 3. 启动前端
```bash
cd frontend
npm install
npm run dev
# 访问: http://localhost:5173
```

## API端点一览
| 模块 | 方法 | 路径 | 说明 |
|------|------|------|------|
| Auth | POST | /api/v1/auth/login | 登录获取JWT |
| Auth | POST | /api/v1/auth/register | 注册 |
| Community | GET | /api/v1/posts | 帖子流(分页,2D封面) |
| Community | GET | /api/v1/posts/{id} | 帖子详情(含3D路径) |
| Community | POST | /api/v1/posts | 发表帖子 |
| Community | POST | /api/v1/posts/{id}/comments | 发表评论 |
| Community | POST | /api/v1/interactions/like | 点赞/取消 |
| Community | POST | /api/v1/users/{id}/follow | 关注/取消 |
| 幻筑 | POST | /api/v1/tasks/huanzhu | 提交幻筑任务(返回202) |
| 幻筑 | GET | /api/v1/tasks/{id}/status | 轮询任务状态 |
| 通知 | GET | /api/v1/notifications | 通知列表 |
| 通知 | GET | /api/v1/notifications/unread-count | 未读计数 |
| 智析 | POST | /api/v1/analysis/zhixi | VGGT分析(限5次/日) |
| 智析 | GET | /api/v1/analysis/zhixi/demos | 演示数据 |
| 预留 | POST | /api/v1/analysis/restoration/predict | 古建修复(返回501) |
| 个人 | GET | /api/v1/users/me | 当前用户信息 |
| 个人 | PUT | /api/v1/users/me | 更新个人资料 |
| 个人 | PUT | /api/v1/users/me/password | 修改密码 |
| 个人 | GET | /api/v1/users/me/posts | 我的帖子 |
| 个人 | GET | /api/v1/users/me/likes | 我的点赞 |
| 管理 | GET | /api/v1/admin/posts/pending | 待审核帖子 |
| 管理 | PUT | /api/v1/admin/posts/{id}/audit | 审核帖子 |

---

## 执行日期: 2026-05-14 (第三批次 — UI 全面优化与大厂级设计规范落地)

### 总体进度
- [x] Task 1: CSS Design Tokens 色彩体系重建 — 精准中国传统色 + 4pt 间距系统 + 中文排版引擎
- [x] Task 2: App.vue 导航栏重构 — 苍穹黑背景 + 64px高度 + 祥瑞红选中下划线
- [x] Task 3: CommunityFeedView CSS Grid 瀑布流架构 — 10px 微小行网格 + JS 动态计算 row-span
- [x] Task 4: HuanZhuView 交互增强 — 左右分栏工作区 + 智能提示标签 (Smart Prompt Chips)
- [x] Task 5: ZhiXiView UI 增强 — 琉璃金虚线拖拽区 + 文化温度提示 + 古塔构建动画 + 保留修复入口
- [x] Task 6: 全部视图样式统一 — 新 Design Tokens 全局应用
- [x] Task 7: API 接口文档完善 — docs/API接口文档.md (7 模块 20+ 端点)
- [x] Task 8: README.md 完整重写 — 项目介绍/架构/功能/技术栈/设计规范
- [x] Task 9: init.sql 增强 — 3 个 VGGT 演示案例 + 演示用户 + 完整注释
- [x] Task 10: 项目进程记录更新

### 执行日志
| 时间 | 任务 | 状态 |
|------|------|------|
| 2026-05-14 15:00 | CSS Design Tokens 全局重构 | 完成 |
| 2026-05-14 15:15 | App.vue 导航栏苍穹黑改造 | 完成 |
| 2026-05-14 15:25 | CommunityFeedView Grid 瀑布流 | 完成 |
| 2026-05-14 15:35 | HuanZhuView Smart Chips 交互 | 完成 |
| 2026-05-14 15:45 | ZhiXiView 虚线拖拽 + 文化提示 | 完成 |
| 2026-05-14 15:55 | 全部视图样式统一 | 完成 |
| 2026-05-14 16:05 | API 文档 / README / init.sql | 完成 |
| 2026-05-14 16:15 | 项目进程记录 | 完成 |

### 详细变更清单

#### 修改文件
| 文件 | 变更内容 |
|------|------|
| `frontend/src/assets/style.css` | 全面重写：精准 Hex/RGB 传统色 (祥瑞红#B8261F/琉璃金#B58E36/碧玉青#2D9B5C/苍黛蓝#002CA2)、4pt 间距系统 (xs→xxl)、跨平台中文字体栈、CSS Grid 瀑布流框架 (.architecture-feed-container)、卡片像素级构造 (8px圆角+柔和阴影+点赞弹跳动画)、玻璃态面板 (.glass-panel)、虚线拖拽上传区 (.dashed-upload-zone)、文化温度提示框 (.cultural-alert)、智能提示标签 (.prompt-chip)、古塔构建动画 (.pagoda-building)、幽灵按钮 (.ghost-btn-disabled)、Element Plus 全局主题覆盖 |
| `frontend/src/App.vue` | 导航栏背景改为苍穹黑 var(--color-header-bg)、高度 64px (var(--spacing-xxl))、导航项 hover/active 琉璃金反馈 + 底部祥瑞红选中下划线、通知铃铛琉璃金边框、用户信息区 hover 微交互、移动端隐藏导航文字 |
| `frontend/src/views/CommunityFeedView.vue` | 瀑布流从 CSS column 改为 Grid 架构 (grid-auto-rows: 10px)、新增 feedContainerRef + cardRefs + calcCardSpans() 动态高度计算、新增 handleLike() 瀑布流内直接点赞、watch posts 触发重算、样式变量全部切换为 Design Tokens |
| `frontend/src/views/HuanZhuView.vue` | 全面重构：左右分栏 (400px 控制区 + 弹性预览区)、多行文本框替代单行输入、新增三组智能提示标签 (朝代/屋顶/色彩) 点击追加、全宽 48px 高圆角执行按钮 "生成三维幻境"、轮询文字循环切换、古塔构建动画、右侧成功/失败/空闲三态视图、玻璃态面板、移动端响应式单列布局 |
| `frontend/src/views/ZhiXiView.vue` | 全面重构：琉璃金虚线拖拽区 (.dashed-upload-zone) + 斗栱图标、文化温度提示框 (RGA(181,142,54,0.08)底+温润文案)、古塔构建分析等待动画、碧玉青边框文化解读卡片、新增保留智能修复 Ghost Button 入口、演示数据列表样式优化 |
| `frontend/src/views/LoginView.vue` | 样式统一：新颜色变量、阴影层级 shadow-modal、间距系统 |
| `frontend/src/views/PostDetailView.vue` | 样式统一：新颜色变量 (surface/阴影/间距/行高变量) |
| `frontend/src/views/NotificationListView.vue` | 样式统一：新颜色+间距变量、未读红点颜色修正 |
| `frontend/src/views/UserProfileView.vue` | 样式统一：封面渐变使用 header-bg 色、新 Design Tokens |
| `frontend/src/views/SettingsView.vue` | 样式统一：新颜色+间距变量 |
| `frontend/src/views/AdminDashboardView.vue` | 样式统一：新颜色+阴影层级+间距变量 |

#### 新增文件
| 文件 | 说明 |
|------|------|
| `docs/API接口文档.md` | 完整 API 文档：认证/社区/个人/幻筑/智析/通知/管理 7 大模块 20+ 端点，含请求/响应示例、错误码表、认证说明 |

#### 更新文件
| 文件 | 变更内容 |
|------|------|
| `README.md` | 完整重写：项目介绍与愿景、完整目录结构、三大核心功能说明、技术栈表、快速启动步骤、Design Tokens 色彩表、4pt 间距表、中文排版规范、API 接口概览表、默认管理员账号 |
| `backend/src/main/resources/init.sql` | 增强：3 个 VGGT 演示案例 (唐代斗栱/宋代歇山顶/佛光寺东大殿)、新增 demo_user 演示账号、完整注释、初始化完成提示 |
| `项目进程.md` | 新增 V1.3 批次记录 |

### UI/UX 设计规范升级要点

#### 色彩体系
- 旧版：泛泛的 #d73c37/#dfbc5e 等非精准色
- 新版：精准传统色映射 (祥瑞红 184,38,31 | 琉璃金 181,142,54 | 碧玉青 45,155,92)
- 新增深色 Header 色 (苍穹黑 #001529)
- Element Plus 组件全局主题覆盖

#### 布局系统
- 旧版：CSS column-count 瀑布流 (渲染顺序问题) + 零散 magic number 间距
- 新版：CSS Grid + JS 动态 row-span 瀑布流 + 4pt 整数倍间距系统

#### 交互体验
- 旧版：基础 Element Plus 样式，缺少文化温感
- 新版：Smart Prompt Chips 快捷填入、古塔构建动画替代枯燥 Loading、文化温度限流提示、玻璃态控制面板、点赞弹跳微交互

#### 文档完备性
- 旧版：脚手架模板级 README
- 新版：完整项目 README + 独立 API 文档 + 增强初始化脚本

### 系统模块交互架构（当前 V1.3）

```
用户浏览器 (Vue 3 :5173)
    │
    ├── 社区瀑布流 (CSS Grid + JS 动态高度)
    │   └── GET /api/v1/posts ──→ Spring Boot (:8080) ──→ MySQL (仅 APPROVED)
    │
    ├── 一键幻筑 POST /huanzhu
    │   ├── 同步返回 taskId + PENDING
    │   ├── Smart Prompt Chips 辅助描述词构建
    │   └── @Async 后台: AncientDictUtil → AI 3D API → SUCCESS → 数字锦盒通知
    │
    ├── 古建智析 POST /zhixi (@RateLimit 5次/日)
    │   ├── 琉璃金虚线拖拽上传
    │   └── RestTemplate ──→ Python FastAPI (:8000) ──→ VGGT 结构推断
    │
    ├── 管理后台 /admin (ADMIN only)
    │   └── 审核帖子: PENDING → APPROVED/REJECTED
    │
    └── 通知中心 /notifications
        └── 数字锦盒送达提醒 + 全部已读
```

---

## 执行日期: 2026-05-14 (第四批次 — 开发规范智能体提示词配置)

### 总体进度
- [x] Task 1: 保存《ArchiVision开发规范智能体提示词》至 prompt/ 目录
- [x] Task 2: 更新项目进程记录

### 执行日志
| 时间 | 任务 | 状态 |
|------|------|------|
| 2026-05-14 16:30 | 智能体提示词文档创建 | 完成 |
| 2026-05-14 16:30 | 项目进程记录更新 | 完成 |

### 详细变更清单

#### 新增文件
| 文件 | 说明 |
|------|------|
| `prompt/ArchiVision开发规范智能体提示词.md` | 适用于 Cursor / GitHub Copilot / 自定义工作流的系统指令。涵盖四大维度：前端 Vue 3 规范 (懒加载/Tree Shaking/纯 Grid 瀑布流)、后端 Spring Boot 规范 (Tomcat 生产级线程池/Micrometer+Prometheus/Redis Lua 幂等锁)、智能引擎调优 (VGGT-Long 分块策略/VGGT-Geo 曼哈顿约束/3D 生成前置背景移除)、回复风格要求 (完整异常处理/dispose() 资源释放/榫卯比喻) |

### 智能体提示词覆盖的规范领域

```
ArchiVision 开发规范智能体
│
├── 前端 Vue 3
│   ├── 组件懒加载 + Composition API Tree Shaking
│   └── CSS Grid 瀑布流 (禁 CSS column 多列)
│
├── 后端 Spring Boot
│   ├── Tomcat max-threads:200 + max-connections:10000
│   ├── Micrometer + Prometheus 健康指标暴露
│   └── Redis + Lua 原子级分布式幂等锁
│
├── 智能引擎调优
│   ├── VGGT-Long: Chunk-Loop-Align 分块策略
│   ├── VGGT-Geo: DINOv3 + Manhattan World 约束
│   └── 3D 生成: 前置 Background Removal 净化
│
└── 回复风格
    ├── 完整异常捕获 + WebGL dispose()
    ├── 古建榫卯/打磨比喻
    └── 性能与画质权衡启发式提问
```

### 待办：按规范推进的下一步方向

根据智能体提示词的指引，下一步可在两个方向中选择推进：

**方向 A — 前端极致轻量化**：
- 路由懒加载改造 (目前 CommunityFeedView 等已是动态 import，可进一步配置 Vite `manualChunks` 分包策略)
- 已验证：瀑布流已使用 CSS Grid + JS 动态 row-span，符合规范要求
- Element Plus 按需导入（当前已配置 `unplugin-vue-components`）

**方向 B — 后端高并发加固**：
- Tomcat 线程池参数生产级调优
- Micrometer + Prometheus 指标暴露端点
- Redis 集成 + Lua 脚本实现 AI 3D 生成接口幂等锁

---

## 执行日期: 2026-05-14 (第五批次 — 后端高并发加固：Redis 幂等锁 + 分布式限流 + Tomcat 调优 + Prometheus)

### 总体进度
- [x] Task 1: pom.xml 增加 Redis / Actuator / Prometheus 依赖
- [x] Task 2: application.yml 生产级配置 — Tomcat 线程池 (max-threads:200, max-connections:10000) + Redis Lettuce 连接池 + Actuator/Prometheus 端点
- [x] Task 3: RedisConfig — RedisTemplate + 两套 Lua 脚本注册 (idempotent_lock.lua / rate_limit.lua)
- [x] Task 4: IdempotentLockService — 基于 userId + SHA-256(prompt) 的幂等守护，tryAcquire → updateLockWithTaskId → release 完整生命周期
- [x] Task 5: TaskOrchestrationServiceImpl 集成幂等锁 — submitHuanZhuTask 前置检查 + executeAsync finally 释放
- [x] Task 6: RateLimitAspect 从 JVM ConcurrentHashMap 升级为 Redis + Lua 分布式限流
- [x] Task 7: SecurityConfig 开放 /actuator/** 供 Prometheus 抓取
- [x] Task 8: HuanZhuView 前端处理 duplicate 响应
- [x] Task 9: Maven 编译验证通过
- [x] Task 10: 项目进程记录更新

### 执行日志
| 时间 | 任务 | 状态 |
|------|------|------|
| 2026-05-14 17:00 | pom.xml 依赖添加 | 完成 |
| 2026-05-14 17:05 | application.yml 生产级配置 | 完成 |
| 2026-05-14 17:10 | RedisConfig + Lua 脚本注册 | 完成 |
| 2026-05-14 17:15 | IdempotentLockService 创建 | 完成 |
| 2026-05-14 17:20 | TaskOrchestrationServiceImpl 集成 | 完成 |
| 2026-05-14 17:25 | RateLimitAspect Redis 升级 | 完成 |
| 2026-05-14 17:30 | SecurityConfig actuator 放行 | 完成 |
| 2026-05-14 17:35 | HuanZhuView 前端 duplicate 处理 | 完成 |
| 2026-05-14 17:40 | Maven 编译验证 | 通过 |

### 详细变更清单

#### 新增文件
| 文件 | 说明 |
|------|------|
| `backend/.../config/RedisConfig.java` | RedisTemplate (Lettuce 连接池) + 两套 Lua 脚本 Bean 注册 (idempotentLockScript / rateLimitScript) |
| `backend/.../service/IdempotentLockService.java` | 幂等守护服务：tryAcquire(userId, prompt) → Redis Lua 原子检查-加锁 → updateLockWithTaskId() 回填 → release() 释放。SHA-256 归一化 Prompt 作为去重维度，TTL 10min |
| `backend/src/main/resources/scripts/idempotent_lock.lua` | 幂等锁 Lua：检查 taskIdKey 是否已有值 → 有则返回 0(拦截) → 无则 SETEX lockKey TTL 返回 1(放行)，原子双 Key 操作 |
| `backend/src/main/resources/scripts/rate_limit.lua` | 分布式限流 Lua：GET 当前计数 → 达上限返回 0 → INCR + 首次设 EXPIRE(次日零点 TTL)，替代原 JVM ConcurrentHashMap |

#### 修改文件
| 文件 | 变更内容 |
|------|------|
| `backend/pom.xml` | 新增 spring-boot-starter-data-redis、spring-boot-starter-actuator、micrometer-registry-prometheus 三个依赖 |
| `backend/.../resources/application.yml` | 新增 server.tomcat.threads (max:200/min-spare:20)、server.tomcat.max-connections:10000、spring.data.redis (Lettuce 连接池)、management.endpoints (health/info/prometheus/metrics) |
| `backend/.../config/SecurityConfig.java` | 新增 .requestMatchers("/actuator/**").permitAll() 供 Prometheus 内网抓取 |
| `backend/.../aop/aspect/RateLimitAspect.java` | 全面重写：移除 ConcurrentHashMap + LocalDate 内存计数，改为注入 RedisTemplate + rateLimitScript，从 SecurityContext 提取 userId 构建用户级 Key，Lua 原子 INCR + TTL |
| `backend/.../service/TaskOrchestrationService.java` | 返回值从 Long 改为 SubmitResult(taskId, duplicate) record，接口语义明确化 |
| `backend/.../service/impl/TaskOrchestrationServiceImpl.java` | 注入 IdempotentLockService；submitHuanZhuTask() 前置 tryAcquire → 重复则返回已有 taskId；executeAsync() 增加 originalPrompt 参数，finally 块 release 幂等锁 |
| `backend/.../controller/HuanZhuController.java` | 适配 SubmitResult，响应新增 duplicate 字段，前端可据此提示用户 |
| `frontend/src/views/HuanZhuView.vue` | handleSubmit 检查 res.data.duplicate，重复时提示"检测到相同的幻筑任务已在处理中" |

### Redis 数据结构设计

```
# 幂等锁
huanzhu:dedup:{userId}:{promptHash}         → "1" (TTL 600s)
huanzhu:dedup:{userId}:{promptHash}:taskId  → "42" (TTL 600s)

# 分布式限流
rate:zhixi:{userId}:{date}                  → "3" (TTL 次日零点)
```

### 幂等守护时序

```
用户提交 Prompt ──→ HuanZhuController
                      │
                      ▼
              IdempotentLockService.tryAcquire(userId, prompt)
                      │
          ┌───────────┴───────────┐
          │ Lua 原子脚本           │
          │ GET taskIdKey          │
          │   ├── 存在 → 返回 0    │────→ 拦截：返回已有 taskId (duplicate=true)
          │   └── 不存在           │
          │       SETEX lockKey    │────→ 放行：创建新任务
          └───────────────────────┘
                      │ (放行)
                      ▼
              MySQL INSERT ai_task (PENDING)
                      │
                      ▼
              updateLockWithTaskId(userId, prompt, taskId)
                      │
                      ▼
              @Async executeAsync(taskId, prompt)
                      │
              PENDING → RUNNING → SUCCESS / FAILED
                      │
                      ▼ (finally)
              release(userId, prompt)  ← 删除 lockKey + taskIdKey
```

### 健康监控端点 (Actuator + Prometheus)

启动后端后可通过以下端点监控：

| 端点 | 说明 |
|------|------|
| `GET /actuator/health` | 应用健康状态（含 Redis/MySQL 连通性） |
| `GET /actuator/info` | 应用信息 |
| `GET /actuator/prometheus` | Prometheus 格式指标（JVM 内存/GC/HTTP 请求/Tomcat 线程池/Redis 连接池） |
| `GET /actuator/metrics` | 全部可用指标列表 |

Prometheus 抓取配置：
```yaml
scrape_configs:
  - job_name: 'zhiguan-gujian'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['localhost:8080']
```

---

## 执行日期: 2026-05-14 (第六批次 — 一键启停脚本)

### 总体进度
- [x] Task 1: 创建 start.bat — 一键启动 VGGT API + 后端 + 前端
- [x] Task 2: 创建 stop.bat — 一键关闭全部服务（端口 PID 定位 + 窗口清理）
- [x] Task 3: 项目进程记录更新

### 执行日志
| 时间 | 任务 | 状态 |
|------|------|------|
| 2026-05-14 18:00 | start.bat 创建 | 完成 |
| 2026-05-14 18:05 | stop.bat 创建 | 完成 |
| 2026-05-14 18:05 | 项目进程记录 | 完成 |

### 新增文件
| 文件 | 说明 |
|------|------|
| `start.bat` | Windows 一键启动脚本。依次启动三个服务窗口：Python VGGT API (uvicorn :8000)、Spring Boot 后端 (mvn :8080)、Vue 前端 (npm run dev :5173)。启动后打印所有访问地址与监控端点 |
| `stop.bat` | Windows 一键关闭脚本。通过 `netstat -ano` 定位端口 8000/8080/5173 的 PID 并 `taskkill /F`，同时清理对应标题的 cmd 窗口 |

### 启停脚本设计

```
start.bat 双击
├── 窗口1: "智观·VGGT-API"     → uvicorn api_server:app --port 8000
├── 窗口2: "智观·后端-SpringBoot" → mvn spring-boot:run (端口 8080)
└── 窗口3: "智观·前端-Vue"       → npm run dev (端口 5173)

stop.bat 双击
├── netstat -ano | findstr ":8000.*LISTENING" → taskkill /PID
├── netstat -ano | findstr ":8080.*LISTENING" → taskkill /PID
├── netstat -ano | findstr ":5173.*LISTENING" → taskkill /PID
└── taskkill /FI "WINDOWTITLE eq 智观·*" → 清理残留窗口
```

### 使用方式

1. **启动**：双击项目根目录的 `start.bat`，三个服务窗口会自动弹出并独立运行
2. **关闭**：双击 `stop.bat`，所有服务进程和窗口将被终止
3. 启动后访问 `http://localhost:5173` 进入前端页面

---

## 执行日期: 2026-05-14 (第七批次 — 全局主题体系重构：中国传统色彩 + 日夜模式 + 设置页改版)

### 总体进度
- [x] Task 1: CSS Design Tokens V2 — 中国传统色彩体系全面升级，新增日夜模式 CSS 变量
- [x] Task 2: 全局日夜切换动画 — transition 0.4s ease 主题平滑过渡
- [x] Task 3: 排版引擎升级 — 引入思源宋体 (Noto Serif SC) 用于标题，思源黑体 (Noto Sans SC) 用于正文
- [x] Task 4: App.vue 主题管理 — provide/inject 主题状态 + localStorage 持久化 + 系统偏好跟随
- [x] Task 5: SettingsView.vue 全面重构 — 卡片式分组布局 + 优雅深色/浅色模式切换开关
- [x] Task 6: Element Plus 夜间模式全局适配
- [x] Task 7: Vite 构建验证通过

### 执行日志
| 时间 | 任务 | 状态 |
|------|------|------|
| 2026-05-14 19:00 | CSS Design Tokens V2 色彩体系重建 | 完成 |
| 2026-05-14 19:10 | App.vue 主题管理逻辑 | 完成 |
| 2026-05-14 19:20 | SettingsView.vue 卡片式重构 | 完成 |
| 2026-05-14 19:25 | Element Plus 夜间模式适配 | 完成 |
| 2026-05-14 19:30 | index.html 字体 CDN 引入 | 完成 |
| 2026-05-14 19:35 | Vite 构建验证 | 通过 |

### 详细变更清单

#### 修改文件
| 文件 | 变更内容 |
|------|------|
| `frontend/index.html` | 新增 Google Fonts CDN 链接：Noto Sans SC (300/400/500/600/700) + Noto Serif SC (400/500/600/700)，preconnect 优化 |
| `frontend/src/assets/style.css` | **全面重写 V2**：<br>• 新增 `[data-theme="dark"]` 完整暗色变量块<br>• 强调色升级：朱砂红 #C93756 (原 #B8261F)、琉璃黄 #E2A323 (原 #B58E36)<br>• 日间背景：宣纸白 #F7F7F5 → 玄青文字 #2C3135<br>• 夜间背景：夜墨 #18181C → #E0E0E0 文字 → #2A2A32 卡片<br>• 新增 `--color-border` / `--color-border-light` 极淡分割线变量<br>• 新增 `--font-family-serif` 思源宋体变量<br>• 全局 `body` + Element Plus 组件 `transition: 0.4s ease` 日夜平滑切换<br>• Element Plus 暗色模式全局覆盖 (el-card/el-input/el-dropdown/el-switch/el-form-item)<br>• 新增 `.settings-card` / `.settings-divider` 设置页专用样式 |
| `frontend/src/App.vue` | 新增主题管理逻辑：<br>• `initTheme()` — localStorage 读取 + 首次访问跟随 `prefers-color-scheme`<br>• `applyTheme(dark)` — 设置 `document.documentElement` data-theme 属性<br>• `toggleTheme()` — 切换日夜模式<br>• `provide('theme', { isDark, toggleTheme })` — 子孙组件注入 |
| `frontend/src/views/SettingsView.vue` | **全面重构**：<br>• 三卡片分组布局：账号设置 / 视觉偏好 / 关于平台<br>• 卡片 12px 圆角 + 柔和阴影 (日间) / 细腻边框 (夜间)<br>• 深色/浅色模式切换开关 (带 Sunny/Moon 图标，inline-prompt)<br>• 每个设置项左侧搭配线性图标 (Phosphor Icons 风格 via Element Plus Icons)<br>• 极淡分割线替代粗黑 `<hr>` / `<el-divider>`<br>• 思源宋体标题 (letter-spacing: 3px 呼吸感)<br>• 响应式移动端适配 |

### 色彩体系 V2 对照表

| 语义 | 日间模式 | 夜间模式 |
|------|----------|----------|
| 主背景 (Background) | `#F7F7F5` 宣纸白 | `#18181C` 夜墨 |
| 卡片/模块 (Surface) | `rgba(255,255,255,0.9)` + blur | `#2A2A32` 瓦灰 |
| 主文字 (Text) | `#2C3135` 玄青 | `#E0E0E0` |
| 次要文字 | `#7A7D82` | `#9A9DA2` |
| 分割线 | `rgba(0,0,0,0.06)` | `rgba(255,255,255,0.06)` |
| 强调色 (Accent) | `#C93756` 朱砂红 | `#C93756` (不变) |
| 高亮色 (Secondary) | `#E2A323` 琉璃黄 | `#E2A323` (不变) |

### 主题切换架构

```
App.vue (provide theme)
    │
    ├── initTheme()
    │   ├── localStorage.getItem('theme') → 'dark'/'light'
    │   └── 首次访问 → matchMedia('prefers-color-scheme: dark')
    │
    ├── applyTheme(dark)
    │   └── document.documentElement.setAttribute('data-theme', 'dark'|'light')
    │
    └── provide('theme', { isDark, toggleTheme })
            │
            └── SettingsView.vue (inject)
                    └── <el-switch :model-value="isDark" @change="toggleTheme" />
```

### 排版规范 V2

| 用途 | 字体 | 说明 |
|------|------|------|
| 页面大标题 | `'Noto Serif SC', serif` | 思源宋体，letter-spacing: 3px |
| 卡片分区标题 | `'Noto Serif SC', serif` | 思源宋体，letter-spacing: 1px |
| 正文 / 表单 | `'Noto Sans SC', sans-serif` | 思源黑体 + 系统降级栈 |
| 代码 / 等宽 | 系统默认等宽字体 | 未指定，用于未来扩展 |

---

## 执行日期: 2026-05-14 (第八批次 — Bug 修复：背景不显示 + 深色模式漏覆盖 + 登录状态丢失)

### 总体进度
- [x] Bug 1: 水墨远山背景不显示 — el-main 默认白色背景遮罩
- [x] Bug 2: 深色模式多处硬编码颜色未适配 CSS 变量
- [x] Bug 3: 登录后刷新/重启 → 用户名丢失显示"用户"
- [x] Bug 4: 管理员刷新后审核工作台入口消失
- [x] Vite 构建验证通过

### Bug 根因分析

| Bug | 症状 | 根因 | 修复方式 |
|-----|------|------|----------|
| **背景不显示** | body 的水墨渐变被完全遮住 | Element Plus `el-main` / `el-container` 默认 `background: #fff` | 全局 CSS 显式设 `.el-main, .el-container { background: transparent !important; }` |
| **深色模式漏覆盖** | 部分区域在夜间模式仍为白色 | 7 处硬编码色值（`#f0f0f0`、`#fafafa`、`#e8e4df`、旧版 primary rgba 等）未使用 CSS 变量 | 全部替换为 `var(--color-*)` 语义变量 |
| **用户名丢失** | 重启后导航栏显示"用户"而非真实昵称 | `App.vue` onMounted 中未调用 `userStore.parseToken()`，仅调了 `fetchUnreadCount()` | 增加 `userStore.parseToken()` 调用 |
| **管理员入口消失** | 管理员刷新后看不到"审核工作台"菜单项 | 同根因 — `parseToken()` 未初始化导致 `role` 停留在默认值 `'USER'` | 同上，`parseToken()` 恢复 `role` → `isAdmin` 自动正确 |

### 硬编码颜色修复清单

| 文件 | 行 | 旧值 | 新值 |
|------|-----|------|------|
| `LoginView.vue` | 97 | `linear-gradient(#f5f0e8, #e8e4df, #d5cfc7)` | `transparent` (穿透 body 水墨背景) |
| `NotificationListView.vue` | 18 | icon `#999` / `#d73c37` | `var(--color-text-muted)` / `var(--color-primary)` |
| `NotificationListView.vue` | 120 | border `#f0f0f0` | `var(--color-border)` |
| `NotificationListView.vue` | 124 | hover `#fafafa` | `var(--color-bg-subtle)` |
| `NotificationListView.vue` | 125 | unread `rgba(184,38,31,0.03)` (旧 primary) | `var(--color-primary-soft)` |
| `CommunityFeedView.vue` | 234 | banner `rgba(0,21,41,0.03)` | `var(--color-bg-subtle)` |
| `CommunityFeedView.vue` | 264 | btn hover `rgba(181,142,54,0.1)` (旧 secondary) | `var(--color-secondary-soft)` |
| `PostDetailView.vue` | 15 | img bg `#e8e4df` | `var(--color-bg-subtle)` |
| `PostDetailView.vue` | 231 | border `#f0f0f0` | `var(--color-border)` |
| `ZhiXiView.vue` | 246 | border `#e8e4df` | `var(--color-border)` |

### 关键代码修复

**App.vue — onMounted 增加 parseToken() 调用：**
```js
onMounted(() => {
  initTheme()
  if (userStore.isLoggedIn) {
    userStore.parseToken()   // ← 新增：从 JWT 恢复 username / role
    notifStore.fetchUnreadCount()
  }
})
```

**style.css — 容器透明化穿透水墨背景：**
```css
.el-main,
.el-container {
  background: transparent !important;
}
```

**水墨渐变透明度上调 (提高可见度)：**
- 日间三层：0.45→0.55 / 0.28→0.35 / 0.20→0.25
- 夜间三层：0.55→0.60 / 0.40→0.45 / 0.30→0.35

---

## 执行日期: 2026-05-14 (第九批次 — 深度修复：背景穿透 + Element Plus 主题桥接 + 毛玻璃效果)

### 总体进度
- [x] Task 1: Element Plus CSS 变量桥接 — 将 Element Plus 内部 `--el-*` 变量映射到我们的设计体系
- [x] Task 2: 卡片半透明化 — `--color-surface` 从实色改为半透明让水墨背景穿透
- [x] Task 3: 毛玻璃效果 — feed-card / settings-card / login-card / notification-list 添加 backdrop-filter
- [x] Task 4: Grid 垂直间距 — `gap: 0` → `gap: var(--spacing-sm)` 让背景在卡片行间可见
- [x] Task 5: 全局透明容器链 — body → #app → el-container → el-main 全链路透明化
- [x] Task 6: 深色模式全覆盖验证 — 检查构建产物中无不适配的硬编码颜色
- [x] Task 7: Vite 构建验证通过

### 根因定位

**Bug 1 根因（背景不显示）**：
1. Element Plus 默认 `--el-bg-color-page: #f2f3f5`（浅灰），容器继承此背景
2. 卡片使用实色 `#FFFFFF` / `#2A2A32` 完全不透明，覆盖身体背景
3. CSS Grid 瀑布流 `gap: 0` 垂直无间距，卡片行间无缝隙露出背景
4. 三个因素叠加 → 水墨远山渐变被完全遮挡

**Bug 2 根因（深色模式漏覆盖）**：
Element Plus 组件使用自有 `--el-*` 变量体系（`--el-bg-color`、`--el-text-color-primary` 等），
我们只覆盖了组件 class 级别的样式，未桥接变量体系 → Element Plus 内部逻辑仍读取默认亮色变量

### 修复方案

**方案一：Element Plus 主题变量桥接**
在 `:root` 和 `[data-theme="dark"]` 中同时定义 20+ 个 `--el-*` 变量，映射到我们的 `--color-*` / `--shadow-*` 体系：
```
--el-bg-color-page: transparent       ← 核心：让页面容器透明
--el-bg-color: var(--color-surface-solid)
--el-bg-color-overlay: var(--color-surface-solid)
--el-text-color-primary: var(--color-text-main)
--el-fill-color-blank: var(--color-surface-solid)
--el-border-color: var(--color-border)
... (共 22 个变量)
```

**方案二：卡片半透明化 + 毛玻璃**
```
日间: --color-surface: rgba(255, 255, 255, 0.78)   (原: 0.9)
夜间: --color-surface: rgba(42, 42, 50, 0.82)       (原: 实色 #2A2A32)
+ backdrop-filter: blur(10px)  ← 毛玻璃模糊，增强层次感
+ -webkit-backdrop-filter: blur(10px) ← Safari 兼容
```

**方案三：Grid 垂直间距**
```
gap: 0 var(--spacing-md)  →  gap: var(--spacing-sm) var(--spacing-md)
                              ↑ 8px 垂直间距让背景透出
```

**方案四：全链路透明容器**
```css
#app,
.el-main,
.el-container,
.el-container > .el-main {
  background: transparent !important;
}
```

### 变更文件清单

| 文件 | 变更内容 |
|------|------|
| `frontend/src/assets/style.css` | • `:root` 新增 22 个 `--el-*` 变量桥接<br>• `[data-theme="dark"]` 新增 22 个 `--el-*` 变量桥接<br>• `--color-surface` 日间改为 `rgba(255,255,255,0.78)`<br>• `--color-surface` 夜间改为 `rgba(42,42,50,0.82)`<br>• `.feed-card` 新增 `backdrop-filter: blur(10px)`<br>• `.settings-card` 新增 `backdrop-filter: blur(10px)`<br>• Grid `gap` 增加垂直 8px 间距<br>• `#app` 加入透明化规则<br>• `body` 增加 `min-height: 100vh` |
| `frontend/src/views/LoginView.vue` | `.login-card` 新增 `backdrop-filter: blur(12px)` |
| `frontend/src/views/NotificationListView.vue` | `.notification-list` 新增 `backdrop-filter: blur(10px)` |

### 视觉效果对比

| 维度 | 修复前 | 修复后 |
|------|--------|--------|
| 社区瀑布流 | 卡片密排，背景不可见 | 卡片间 8px 间距露出水墨远山渐变 |
| 卡片质感 | 实色纯白，平面无层次 | 毛玻璃半透明，远山光影穿透 |
| 登录/设置页 | 卡片不透明，背景浪费 | 磨砂玻璃悬浮于远山之上 |
| 深色模式 | 多处白色区域未适配 | Element Plus 全组件暗色统一 |
| 日夜切换 | 色彩跳变 | 0.4s 平滑过渡，渐变层叠切换 |

---

## 执行日期: 2026-05-14 (第十批次 — 前端全量重构：侧边栏布局 + 毛玻璃设计语言 + V3 色彩体系)

### 设计参考
基于 `prompt/qianduan.txt` React 设计稿，将其设计语言完整翻译到 Vue 3 项目。

### 总体进度
- [x] Task 1: style.css 设计体系 V3 — stone 色系 + 琥珀强调 + 月季红 + 噪声纹理 + 环境光晕
- [x] Task 2: App.vue 侧边栏布局 — 256px 侧边栏 + Logo + 导航 + 用户弹出菜单 + 主题切换
- [x] Task 3: CommunityFeedView 重写 — 毛玻璃卡片 + 标签浮层 + 搜索框 + 24px 圆角
- [x] Task 4: PostDetailView 重写 — 沉浸式头图 + 毛玻璃正文卡 + 悬浮返回栏
- [x] Task 5: ZhiXiView 重写 — 状态胶囊 + 扫描线动画 + 双列结果面板
- [x] Task 6: HuanZhuView 重写 — 毛玻璃控制面板 + 网格预览区 + 渐变按钮
- [x] Task 7: LoginView 更新 — 毛玻璃登录卡 + font-serif 标题
- [x] Task 8: UserProfileView / AdminDashboardView / NotificationListView 色彩适配
- [x] Task 9: Vite 构建验证通过

### 执行日志
| 时间 | 任务 | 状态 |
|------|------|------|
| 2026-05-14 21:30 | style.css V3 重写 | 完成 |
| 2026-05-14 21:40 | App.vue 侧边栏重构 | 完成 |
| 2026-05-14 21:50 | 4 大视图重写 | 完成 |
| 2026-05-14 22:00 | 4 小视图适配 | 完成 |
| 2026-05-14 22:05 | 图标兼容修复 + 构建验证 | 通过 |

### 架构变化

```
修复前 (顶部导航):                 修复后 (侧边栏沉浸式):
┌──────────────────────┐          ┌──────┬─────────────────┐
│   苍穹黑 Header 64px  │          │ 噪声纹理全覆盖          │
│ Logo  社区 幻筑 智析 用户│          │ 环境光晕 (玫瑰+琥珀)    │
├──────────────────────┤          │      │                 │
│                      │          │ 侧边 │  主内容区        │
│   主内容区            │          │ 栏   │  (flex-1)       │
│   (router-view)      │          │ 260px│  各视图         │
│                      │          │ Logo │  毛玻璃卡片      │
│                      │          │ 导航 │  24px 圆角      │
│                      │          │ ...  │                 │
│                      │          │ 用户 │                 │
│                      │          │ 菜单 │                 │
└──────────────────────┘          └──────┴─────────────────┘
```

### 设计体系 V3

| 语义 | 日间 | 夜间 |
|------|------|------|
| 背景基底 | `#F4F1EB` 暖宣纸 | `#0C0A09` 墨石 |
| 卡片表面 | `rgba(255,255,255,0.6)` + blur(12px) | `rgba(41,37,36,0.35)` + blur(12px) |
| 主文字 | `#292524` stone-800 | `#E7E5E4` stone-200 |
| 强调色 | `#D97706` 琥珀金 | `#F59E0B` 亮琥珀 |
| 次强调 | `#E11D48` 月季红 | `#E11D48` 月季红 |
| 渐变按钮 | `linear-gradient(琥珀→月季)` | `linear-gradient(琥珀→月季)` |
| 圆角 | 24px (rounded-3xl) | 24px |
| 纹理 | 噪点 SVG filter + 双色光晕 blur(120px) | 噪点 + 光晕 (screen 混合模式) |

### 文件变更清单

| 文件 | 改动量 | 核心变更 |
|------|:--:|------|
| `style.css` | 完全重写 | 新色系/噪声纹理/光晕/毛玻璃/动画/Element Plus桥接/滚动条 |
| `App.vue` | 完全重写 | 顶部导航→侧边栏：Logo/导航/用户弹出菜单/主题切换/移动端折叠 |
| `CommunityFeedView.vue` | 重写 | 搜索框/玻璃卡片/标签浮层/24px圆角/hover标题变色 |
| `PostDetailView.vue` | 重写 | 沉浸头图/悬浮返回栏/毛玻璃正文卡/互动按钮/评论区 |
| `ZhiXiView.vue` | 重写 | 状态胶囊/扫描线动画/双列结果/颜色变量化 |
| `HuanZhuView.vue` | 重写 | 毛玻璃面板/网格预览/渐变按钮/空闲-生成-完成三态 |
| `LoginView.vue` | 中等 | 毛玻璃卡/font-serif/amber色系 |
| `UserProfileView.vue` | 中等 | 毛玻璃卡/新色系 |
| `AdminDashboardView.vue` | 中等 | 毛玻璃卡/新色系 |
| `NotificationListView.vue` | 中等 | 毛玻璃卡/amber强调色 |
| `SettingsView.vue` | 小 | 硬编码色修复 |
| `index.html` | 不变 | 字体链接已存在 |
